<UserControl x:Class="StudentReminderApp.Views.Pages.ForumPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:StudentReminderApp.ViewModels"
             mc:Ignorable="d"
             Background="#F0F2F5">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:ForumViewModel />
    </UserControl.DataContext>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Border CornerRadius="0,0,20,20" Background="#2D3436" ClipToBounds="True">
                    <Grid>
                        <Image Source="/Resources/Images/APUIp.jpg" Stretch="Uniform" MaxHeight="350"/>
                        <StackPanel VerticalAlignment="Bottom" Margin="40,0,40,20">
                            <TextBlock Text="Nhóm Sinh viên Đại học" FontSize="32" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text="Chia sẻ kiến thức • Kết nối bạn bè" FontSize="14" Foreground="#E2E8F0"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border MaxWidth="750" Background="White" CornerRadius="12" Margin="0,20" BorderBrush="#E2E8F0" BorderThickness="1">
                    <Grid Margin="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="45" Height="45" CornerRadius="22.5" ClipToBounds="True" Margin="0,0,12,0">
                            <Image Source="/Resources/Images/user.png" Stretch="UniformToFill"/>
                        </Border>
                        <Border Grid.Column="1" Background="#F0F2F5" CornerRadius="22" Cursor="Hand" MouseLeftButtonUp="OpenCreatePost_Click">
                            <TextBlock Text="Bạn đang nghĩ gì?" VerticalAlignment="Center" Margin="15,0" Foreground="#65676B" FontSize="15"/>
                        </Border>
                        <Button Grid.Column="2" Content="Tạo bài viết" Click="OpenCreatePost_Click" Margin="12,0,0,0" Background="#1877F2" Foreground="White" FontWeight="Bold" Cursor="Hand" Padding="15,5">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="18">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="15,5"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Border>

                <ItemsControl ItemsSource="{Binding Posts}" MaxWidth="750" Margin="0,0,0,30">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" Margin="0,0,0,15" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1">
                                <StackPanel Margin="15">
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="40" Height="40" CornerRadius="20" ClipToBounds="True">
                                            <Image Source="{Binding DisplayAvatar}" Stretch="UniformToFill"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="15" Foreground="#050505"/>
                                            <TextBlock Text="{Binding TimeAgo}" FontSize="12" Foreground="#65676B"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="✕" 
                                                Visibility="{Binding IsMyPost, Converter={StaticResource BooleanToVisibilityConverter}}" 
                                                Command="{Binding DataContext.DeletePostCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                CommandParameter="{Binding}" 
                                                Background="Transparent" BorderThickness="0" Foreground="#65676B" Cursor="Hand" FontWeight="Bold"/>
                                    </Grid>
                                    
                                    <TextBlock Text="{Binding Content}" FontSize="15" TextWrapping="Wrap" Margin="0,0,0,12" Foreground="#050505"/>
                                    
                                    <Border Margin="0,0,0,12" CornerRadius="10" BorderBrush="#CED0D4" BorderThickness="1" Background="#F8F9FA">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsShared}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Margin="12">
                                            <TextBlock FontWeight="Bold" Text="{Binding OriginalPost.DisplayName}" Margin="0,0,0,5" Foreground="#050505"/>
                                            <TextBlock Text="{Binding OriginalPost.Content}" TextWrapping="Wrap" LineHeight="20" Foreground="#050505"/>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="{Binding Likes, StringFormat='👍 {0} lượt thích'}" FontSize="13" Foreground="#65676B" Margin="0,5"/>
                                    <Separator Background="#E2E8F0" Margin="0,5"/>
                                    
                                    <UniformGrid Columns="3">
                                        <Button Command="{Binding DataContext.LikeCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                                                CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Padding="10" Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Foreground" Value="#65676B"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsLiked}" Value="True">
                                                            <Setter Property="Foreground" Value="#1877F2"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="👍 "/>
                                                <TextBlock Text="Thích">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsLiked}" Value="True">
                                                                    <Setter Property="FontWeight" Value="Bold"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </StackPanel>
                                        </Button>

                                        <Button Content="💬 Bình luận" Click="CommentButton_Click" Background="Transparent" BorderThickness="0" Padding="10" Foreground="#65676B" Cursor="Hand"/>
                                        
                                        <Button Click="ShareButton_Click" CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Padding="10" Foreground="#65676B" Cursor="Hand">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="↪️ Chia sẻ"/>
                                            </StackPanel>
                                        </Button>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>