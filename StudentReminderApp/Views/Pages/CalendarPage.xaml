<Page x:Class="StudentReminderApp.Views.Pages.CalendarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Lịch" Background="White">

    <Page.Resources>
        <Style x:Key="ViewToggleStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" CornerRadius="6" Background="Transparent" Padding="12,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E8F0FE"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="#1A73E8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="" FontFamily="Segoe MDL2 Assets" Click="BtnPrev_Click" Width="32" Height="32" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                <Button x:Name="BtnMonthSelector" Click="BtnMonthSelector_Click" Background="Transparent" BorderThickness="0">
                    <TextBlock x:Name="TxtMonthYear" FontSize="20" FontWeight="SemiBold" Foreground="#3C4043" Margin="10,0"/>
                </Button>
                <Button Content="" FontFamily="Segoe MDL2 Assets" Click="BtnNext_Click" Width="32" Height="32" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                <Button Content="Hôm nay" Margin="20,0,0,0" Click="BtnToday_Click" Padding="10,2"/>
            </StackPanel>

            <Border HorizontalAlignment="Right" Background="#F1F3F4" CornerRadius="8" Padding="2">
                <StackPanel Orientation="Horizontal">
                    <RadioButton x:Name="RbMonthView" Content="Tháng" IsChecked="True" GroupName="ViewMode" Width="80" Height="32" Style="{StaticResource ViewToggleStyle}" Click="SwitchView_Click"/>
                    <RadioButton x:Name="RbScheduleView" Content="Ngày" GroupName="ViewMode" Width="80" Height="32" Style="{StaticResource ViewToggleStyle}" Click="SwitchView_Click"/>
                </StackPanel>
            </Border>
        </Grid>

        <StackPanel Grid.Row="1" Margin="65,10,0,20" x:Name="HeaderDateInfo" Visibility="Collapsed">
            <TextBlock x:Name="TxtCurrentDayName" Text="THỨ" FontSize="12" FontWeight="Bold" Foreground="#70757A" Margin="10,0,0,0"/>
            <TextBlock x:Name="TxtCurrentDayNumber" Text="1" FontSize="32" Foreground="#3C4043"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid x:Name="MonthViewGrid" Visibility="Visible">
                <UniformGrid x:Name="CalendarGrid" Columns="7" Rows="6"/>
            </Grid>

            <ScrollViewer x:Name="ScheduleViewScroll" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
                <Grid x:Name="TimeGridContainer">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="65"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ItemsControl x:Name="HourLinesControl" Grid.Column="0" Grid.ColumnSpan="2">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Height="60">
                                    <TextBlock Text="{Binding HourText}" FontSize="11" Foreground="#70757A" VerticalAlignment="Top" Margin="15,-7,0,0"/>
                                    <Separator VerticalAlignment="Top" Margin="65,0,0,0" Background="#E8EAED"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Canvas Grid.Column="1" x:Name="CurrentTimeCanvas" IsHitTestVisible="False" Panel.ZIndex="10">
                        <Line x:Name="CurrentTimeLine" X1="0" X2="2000" Stroke="#EA4335" StrokeThickness="2" Visibility="Collapsed"/>
                        <Ellipse x:Name="CurrentTimeDot" Fill="#EA4335" Width="10" Height="10" Margin="-5,-4,0,0" Visibility="Collapsed"/>
                    </Canvas>

                    <Canvas x:Name="EventsCanvas" Grid.Column="1" Margin="5,0,20,0" 
                            Background="Transparent" 
                            Height="1440"
                            Panel.ZIndex="99"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Top"/>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>