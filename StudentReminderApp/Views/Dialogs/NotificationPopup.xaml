<Window x:Class="StudentReminderApp.Views.Dialogs.NotificationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent"
        Height="100" Width="320"
        ShowInTaskbar="False" Topmost="True"
        WindowStartupLocation="Manual">
    <Border Background="White" CornerRadius="12"
            BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
            Padding="16,12">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="4" BlurRadius="20" Opacity="0.15"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="36"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="24"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="🔔" FontSize="22" VerticalAlignment="Top" Margin="0,2,0,0"/>
            <StackPanel Grid.Column="1" Margin="8,0">
                <TextBlock x:Name="TxtPopupTitle" FontWeight="SemiBold" FontSize="13"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           TextTrimming="CharacterEllipsis"/>
                <TextBlock x:Name="TxtPopupBody" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           TextWrapping="Wrap" Margin="0,4,0,0"/>
            </StackPanel>
            <Button Grid.Column="2" Content="✕" Background="Transparent" BorderThickness="0"
                    Foreground="{StaticResource TextMutedBrush}"
                    VerticalAlignment="Top" Cursor="Hand"
                    Click="BtnClose_Click" FontSize="12"/>
        </Grid>
    </Border>
</Window>
