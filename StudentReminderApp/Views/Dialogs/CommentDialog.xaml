<Window
    x:Class="StudentReminderApp.Views.Dialogs.CommentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="commentWindow"
    Title="Chi tiết bài viết"
    Width="500"
    Height="650"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStartupLocation="CenterOwner"
    WindowStyle="None">

    <Border
        Background="White"
        BorderBrush="#E2E8F0"
        BorderThickness="1"
        CornerRadius="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border
                Grid.Row="0"
                Padding="15"
                Background="White"
                BorderBrush="#E2E8F0"
                BorderThickness="0,0,0,1"
                CornerRadius="15,15,0,0">
                <Grid>
                    <TextBlock
                        HorizontalAlignment="Center"
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="#050505"
                        Text="Chi tiết bài viết" />
                    <Button
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="CloseButton_Click"
                        Content="✕"
                        Cursor="Hand"
                        FontSize="18"
                        FontWeight="Bold"
                        Foreground="#65676B" />
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="0,0,0,15">
                    <StackPanel>
                        <Grid Margin="15,15,15,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="40" Height="40" ClipToBounds="True" CornerRadius="20">
                                <Image Source="{Binding CurrentPost.DisplayAvatar}" Stretch="UniformToFill" />
                            </Border>
                            <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                                <TextBlock FontSize="15" FontWeight="Bold" Foreground="#050505" Text="{Binding CurrentPost.DisplayName}" />
                                <TextBlock FontSize="12" Foreground="#65676B" Text="{Binding CurrentPost.TimeAgo}" />
                            </StackPanel>
                        </Grid>

                        <Border Padding="15,5,15,10">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentPost.IsColored}" Value="True">
                                            <Setter Property="Background" Value="{Binding CurrentPost.BackgroundColor}" />
                                            <Setter Property="MinHeight" Value="250" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CurrentPost.Content}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding CurrentPost.Content}" Value="">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock LineHeight="24" Text="{Binding CurrentPost.Content}" TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontSize" Value="15" />
                                        <Setter Property="Foreground" Value="#050505" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentPost.IsColored}" Value="True">
                                                <Setter Property="FontSize" Value="24" />
                                                <Setter Property="FontWeight" Value="Bold" />
                                                <Setter Property="Foreground" Value="White" />
                                                <Setter Property="TextAlignment" Value="Center" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>

                        <Border Margin="0,5,0,0" Background="#F0F2F5">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentPost.ImagePaths.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Image MaxHeight="450" Source="{Binding CurrentPost.ImagePaths[0]}" Stretch="Uniform" />
                        </Border>
                    </StackPanel>

                    <Separator Height="8" Margin="0,15,0,0" Background="#F0F2F5" />

                    <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding Comments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="15,5,15,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Border Width="32" Height="32" VerticalAlignment="Top" ClipToBounds="True" CornerRadius="16">
                                        <Image Source="/Resources/Images/user.png" Stretch="UniformToFill" />
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                        <Border Padding="12,8" HorizontalAlignment="Left" Background="#F0F2F5" CornerRadius="18">
                                            <StackPanel MaxWidth="300">
                                                <TextBlock FontSize="13" FontWeight="Bold" Foreground="#050505" Text="{Binding AuthorName}" />
                                                <TextBlock FontSize="14" Foreground="#050505" Text="{Binding Content}" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Border>
                                        <TextBlock Margin="10,2,0,0" FontSize="11" Foreground="#65676B" Text="{Binding TimeDisplay}" />
                                    </StackPanel>

                                    <Button Grid.Column="2" 
                                            VerticalAlignment="Top" 
                                            Background="Transparent" 
                                            BorderThickness="0" 
                                            Content="✕" 
                                            FontSize="14" 
                                            Foreground="#65676B"
                                            Cursor="Hand"
                                            Margin="5,5,0,0"
                                            Command="{Binding DataContext.DeleteCommentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Foreground" Value="Red" />
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Padding="12,8" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" CornerRadius="0,0,15,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Border Width="32" Height="32" Margin="0,0,10,0" ClipToBounds="True" CornerRadius="16">
                        <Image Source="/Resources/Images/user.png" Stretch="UniformToFill" />
                    </Border>
                    
                    <TextBox Grid.Column="1" Padding="15,8" VerticalAlignment="Center" Background="#F0F2F5" BorderThickness="0" FontSize="14"
                             Text="{Binding CommentText, UpdateSourceTrigger=PropertyChanged}">
                        <TextBox.Resources>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="CornerRadius" Value="18" />
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                    
                    <Button Grid.Column="2" Margin="10,0,0,0" Padding="15,0" Background="Transparent" BorderThickness="0"
                            Command="{Binding SendCommentCommand}" Content="Gửi" Cursor="Hand" FontWeight="Bold" Foreground="#1877F2" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>